<!DOCTYPE html>
html(lang="en" class="light")
    head
        meta(charset="UTF-8")
        title Cosmos
        link(rel="stylesheet", href="../css/pikaday.css")
        link(rel="stylesheet", href="../css/dragula.css")
        link#theme(rel="stylesheet", href="../css/main.css")
        script(src="http://www.getcosmos.space:12345/socket.io/socket.io.js")
        script(src="../js/socket.js")
        
    body
        div#app
            div.navbar-drag
            div.menu#menu(v-if="platform == 'win32'")
                svg.hamburger(@click="showMenu")
                    use(href="../img/windows.svg#hamburger-menu")
            div.window-controls#captionbuttons(v-if="platform == 'win32'")
                svg.window-control(@click="minimize")
                    use(href="../img/windows.svg#minimize-window")
                svg.window-control(@click="maximize")
                    use(href="../img/windows.svg#maximize-window")
                svg.window-control(@click="close")
                    use(href="../img/windows.svg#close-window")
            navbar
            router-view

        script(src="../js/bundle.js")

        script require('electron-connect').client.create()
        
        
